# Dominio definitivo (canonical, OG, sitemap)
PUBLIC_SITE_URL="https://urbanusqhse.com.ar"

# Destino de los leads del formulario (PENDIENTE de definir con el cliente).
# Acepta cualquier endpoint que reciba JSON por POST: Web3Forms, Formspree,
# n8n / Make / Zapier webhook, o una API route propia.
# Si queda vacío, el formulario cae automáticamente a WhatsApp con el mensaje precargado.
PUBLIC_FORM_ENDPOINT=""

# Google Analytics / Tag Manager (opcional)
PUBLIC_GTM_ID=""
